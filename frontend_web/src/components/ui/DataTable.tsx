export interface Column<T> { key: keyof T | string; header: string; render?: (row: T) => React.ReactNode; }
export function DataTable<T extends { id?: number | string }>({ columns, rows }: { columns: Column<T>[]; rows: T[] }) { return <div className="table-scroll rounded-2xl border border-slate-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map(c => <th key={String(c.key)} className="px-4 py-3 font-bold">{c.header}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row, idx) => <tr key={row.id ?? idx} className="hover:bg-slate-50">{columns.map(c => <td key={String(c.key)} className="px-4 py-4 text-slate-700">{c.render ? c.render(row) : String((row as any)[c.key] ?? '-')}</td>)}</tr>)}</tbody></table></div>; }
