# Camada de compatibilidade. O gerenciador oficial agora é core.context.
from core.context import get_active_event, get_active_event_id, get_current_event_id, set_active_event_id, set_current_event_id
